---
status: accepted
---

# 使用持久化且有界的下载队列

下载任务以“排队、解析音质、下载、写标签、入库、完成、失败、已取消”表达生命周期，状态和必要进度持久化到 SQLite；默认最多同时执行两首，并允许通过环境变量调整。操作者可以取消排队或进行中的任务并单独重试失败项；容器异常退出后，未完成任务恢复到可安全重试的排队状态。终态任务形成持久任务历史并可由操作者清除，但清除只移除历史与已过期临时产物，绝不删除服务器音乐库文件；入库索引长期保留。取消、失败或崩溃发生在入库发布前时只清理临时产物，不留下 Navidrome 可见的半成品。
