---
status: accepted
---

# 不把下载器扩展成音乐库管理器

首期只新增 musicdl-web 自己创建的入库产物，并允许操作者明确触发的音质升级原子替换；不提供删除、移动、重命名、标签编辑或全库整理能力。系统只对入库索引登记的受管产物执行替换，服务器音乐库中未登记的文件一律视为非受管文件，不因重名、扫描、对账或清理而被修改或删除。启动时和展示“已入库”前只校验登记路径是否存在；路径缺失时标记“文件缺失”并允许重新下载，但不自动重建、不扫描全库猜测新位置，也不修改 Navidrome。
