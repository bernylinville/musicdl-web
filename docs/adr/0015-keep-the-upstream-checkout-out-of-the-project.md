---
status: accepted
---

# 不将上游 musicdl 源码纳入项目

工作目录中的 `musicdl/` 是开发期间核对上游行为与文档的本地参考副本，不属于 `musicdl-web` 的源码、版本控制、构建上下文、容器镜像或发布产物；实现阶段必须显式排除该目录，也不得从中复制未审查的实现代码。生产环境从 PyPI 安装并在 lockfile 中固定 `musicdl==2.13.4`，而不是依赖开发机上的本地检出；任何版本升级都必须重新验证网易云、QQ、登录会话、逐歌曲音质和 Navidrome 入库契约。
