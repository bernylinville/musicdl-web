---
status: accepted
---

# musicdl-web 验收清单

## 自动化证据

- [x] 网易云与 QQ 的搜索、分页、来源隔离和稳定内部模型通过 fixture 契约测试（证据见 `docs/spikes/2026-07-31-search-metadata.md`）。
- [x] 网易云与 QQ 搜索结果显示同源代理封面；原始来源 URL 不下发浏览器，代理以 2 MiB 为流式上限，只接受有效 JPEG 并使用有 TTL 的缓存；单张封面失败时只显示占位符且搜索仍成功。
- [x] 同一份已验证封面用于搜索展示、音频内嵌和服务器 `cover.jpg`；封面失败时音频仍成功且任务明确告警“音频成功，封面缺失”。
- [x] 音质快照绑定平台会话版本与过期时间；请求与响应档位不一致时返回明确失败。
- [x] 网络传输同时校验初始请求与重定向域名，拒绝第三方解析域名、关闭 TLS 验证和共享 Cookie。
- [x] 实验性扫码状态机覆盖等待、已扫码、待确认、成功、拒绝、过期、取消和网络错误；终态清除临时令牌。
- [x] 网易云扫码使用官方 HTTPS 流程；二维码图片 URL 不透明、响应为 `no-store`，前端每秒轮询，过期后停止轮询并只在操作者点击后刷新。
- [x] 网易云扫码成功后使用同一 Cookie jar 验证账号，再原子替换 AES-GCM 加密会话；取消、过期或失败时保留旧会话。
- [x] 手动 Cookie 导入会实时验证；验证成功后原子替换加密会话，验证失败时保留旧会话。
- [x] 日志、异常、API、SQLite 和前端状态不含 Cookie、token、QR 内容或来源 URL。
- [x] 会话密文可跨重启恢复，错误密钥安全失败，退出登录清除对应平台会话。
- [x] 队列覆盖并发上限、取消、部分成功、失败重试、崩溃恢复和临时文件清理。
- [x] 下载覆盖过期 URL、302 到禁用域名、HTML 响应、试听片段、截断文件、401/403 和音质降级。
- [x] 媒体探测验证容器、codec、时长和所选音质；保留原编码且不转码。
- [x] Navidrome 标签、内嵌封面、`cover.jpg`、同名 `.lrc`、路径规范化、冲突后缀和原子发布通过测试。
- [x] 去重、音质升级、文件缺失和非受管文件保护通过回归测试。
- [x] 浏览器取回逐文件交付且不写入入库索引；服务器保存正确写入 `/music`。
- [x] Vue 工作台覆盖双来源分组、按需音质、短试听、跨组多选、队列操作与任务历史。
- [x] 前后端类型检查、lint、单元测试、集成测试、生产构建和 Compose 配置校验全部通过。
- [x] 容器以 UID/GID `1000:1000` 运行，健康检查监听容器端口 4534，根文件系统只读且 `/music` 可写。

## 现场账号验收

- [x] NAS live smoke 完成网易云二维码启动、同源 SVG 图片、等待态轮询和取消；二维码响应不暴露上游内容。
- [ ] 操作者使用真实手机完成网易云扫码、手机确认、成功态轮询、会话加密保存和退出清除；接口 smoke 不计为本项通过。
- [ ] 操作者现场验证网易云二维码过期后停止轮询并显示“刷新二维码”，只有点击后才生成下一张。
- [ ] 已有有效网易云会话时现场重新扫码，新会话验证成功前保留旧会话；取消、过期或失败后旧会话继续可用。
- [x] QQ 扫码入口明确显示“尚未支持”；QQ 精确音质和下载签名验证前不阻塞本轮发布。
- [ ] 操作者在 `http://192.168.50.10:4534` 页面导入本人网易云会话，凭据不经过聊天、日志、API 或 SQLite。
- [ ] 操作者以相同方式验证 QQ 页面会话导入；QQ 精确音质/下载当前因动态签名缺口返回 `503`。
- [ ] 两个平台都能显示会话状态、执行搜索、按需列出当前会话可获取音质并播放合法短试听。
- [ ] 网易云至少完成一首本人权益内歌曲的精确音质服务器保存；失败时展示可解释错误且不降级。
- [ ] QQ 动态签名实现并经本人会话验证后，完成相同精确音质服务器保存。
- [ ] 至少完成一次浏览器单曲取回和一次多曲逐文件取回。

当前 NAS smoke：网易云搜索返回 20 条结果，第一条同源封面返回 `200 image/jpeg` 且 API 不暴露来源 URL；二维码启动、SVG 图片、等待态轮询和取消通过；界面使用“导入登录 Cookie”。真实手机扫码与确认尚未执行。QQ 扫码仍明确显示“尚未支持”，QQ 精确下载和 preview 仍缺现场证据。这些边界不计为现场账号验收通过。

## NAS 发布验收

- [x] Y9000P 构建固定版本 `musicdl-web:0.2.2-20260731`（`linux/amd64`）并生成 SHA-256。
- [x] 镜像导入 NAS 后，`/data/docker/musicdl-web` 的 Compose 只启动本项目单容器。
- [x] `http://192.168.50.10:4534` 健康可访问，空状态容器重启后恢复健康。
- [ ] 在存在真实会话和排队任务时重启容器，验证会话、队列和入库索引恢复。
- [x] Navidrome 容器、Compose、端口、数据卷和配置未被修改。
- [ ] 通过本人合法下载把成品写入 `/data/media/music`。
- [ ] Navidrome 通过现有 watcher 或一小时计划扫描发现入库歌曲，标签、封面与歌词正确。
- [x] 部署后检查容器日志无秘密值、无持续错误，日志轮转、重启策略和 volume 均符合 NAS 约定。
- [x] 保留上一版 `0.2.1` 镜像、归档和配置备份作为回退输入。

发布归档为 `/data/docker/musicdl-web/releases/musicdl-web_0.2.2-20260731_linux-amd64.tar`，SHA-256 为 `acc5d9ab10af45b44ec294401d065c238e638ce4f4ebdb6f725e2ccfad9af436`。部署后服务健康；Navidrome 的容器 ID、启动时间和重启次数未变，本次发布没有修改或重启 Navidrome。

## 完成条件

只有双平台功能、自动化门禁、Y9000P 构建、NAS 导入部署和现场账号验收全部通过，才可宣称一期完成。当前 QQ 扫码尚未支持，QQ 精确音质/下载和 preview 仍是明确缺口；网易云真实手机扫码与确认、本人会话精确下载、浏览器取回和 Navidrome 发现仍是现场证据缺口。
