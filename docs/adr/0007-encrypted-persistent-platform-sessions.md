---
status: accepted
---

# 加密并持久化平台会话

网易云音乐与 QQ 音乐的平台会话跨容器重启保存，使 Compose 更新或重启后无需重复扫码；会话使用认证加密写入独立持久化凭据 volume，密钥通过 Compose secret 注入，不与密文存放在同一数据文件，也不得进入镜像、日志、命令行参数或 API 响应。系统必须支持检测失效、替换会话，以及通过“退出登录”立即清除对应音乐源的已保存会话。
