---
status: accepted
---

# 使用人类可读的音乐库路径

有完整专辑信息的入库产物使用 `/music/{专辑艺术家}/{专辑} ({年份})/{碟号-曲号} - {歌名}.{扩展名}`，缺少专辑时使用 `/music/{歌手}/Singles/{歌名}.{扩展名}`；缺失年份、碟号或曲号时省略对应片段，而不是写入伪造占位值。只有规范化后发生路径冲突时才追加 `[netease-ID]` 或 `[qq-ID]`。路径便于人工浏览但不承担身份语义，Navidrome 分组仍完全依赖标签，平台歌曲身份仍由音乐源与平台 ID 决定。
