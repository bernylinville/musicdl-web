---
status: accepted
---

# 区分服务器持久保存与浏览器临时取回

每次下载可选择“保存到服务器”或“浏览器取回”，前者为默认值并将产物写入 Docker Compose 显式挂载的服务器音乐库；后者只在后端临时工作区生成产物，通过浏览器交付后按过期策略清理，不进入服务器音乐库。浏览器取回批次不生成 ZIP 等归档包：“下载全部”逐个触发每首成功歌曲的独立文件，失败项单独重试，并保留逐首取回入口。两种方式复用同一下载流程，但持久目录与临时目录必须分离，避免容器重建丢失音乐库或临时文件无限积累。
