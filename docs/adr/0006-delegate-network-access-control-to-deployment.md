---
status: accepted
---

# 将访问控制交给部署网络

应用不实现本地账号、访问密码或来源网络限制，Docker Compose 按常规方式暴露 Web 端口；操作者负责通过内网、VPN、防火墙或反向代理控制访问。在可信局域网或 VPN 内，下载工作台可以使用 HTTP；这不放宽后端访问音乐源和手机扫码目标必须使用 HTTPS 的边界。由于页面和 API 能使用高敏感的平台会话并触发下载，文档必须明确标注“无内置身份验证、不得直接暴露到不受信任网络”，且实现不得把未配置访问控制误报为安全部署。
