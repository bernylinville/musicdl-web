---
status: accepted
---

# 只使用公开能力或操作者自己的账号权益

首期同时接入网易云音乐与 QQ 音乐，下载只能来自音乐源公开提供的能力，或操作者自身平台账号当前拥有的权益；不把共享会员、第三方 VIP 解析、付费墙或 DRM 绕过作为产品能力。当所需内容或音质不在这两类授权范围内时，系统返回“不可获取”而不是静默切换到绕过路径。由于 `musicdl` 的默认下载流程会探测多个第三方解析服务，平台适配层不得直接依赖该默认行为，必须将允许使用的获取路径收窄到上述边界。
