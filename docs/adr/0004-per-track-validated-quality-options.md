---
status: accepted
---

# 按歌曲提供经过验证的音质选项

音质不是全局配置：系统针对每首歌曲和当前平台会话解析“可选音质”，前端只展示已经确认可获取的档位；搜索先返回歌曲信息，进入可见区域或被选中的歌曲再异步解析音质并短暂缓存，避免一次搜索触发大量平台请求。操作者选定后，下载前再次校验并严格请求该音质；如果选项因权益、地域或链接时效变化而失效，系统刷新选项并明确失败，不静默降级。`musicdl` 的公开 API 只会从高到低探测并返回单个结果，因此平台适配层需要增加音质枚举与精确请求能力，不能把现有探测行为直接暴露为这个契约。
